kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: alpine
    commands:
      - echo "Hello, Drone!"
      - echo "This is a test pipeline."
      - uname -a
      - pwd
      - ls -la

  - name: notify
    image: alpine
    commands:
      - echo "Pipeline completed successfully!"
    when:
      status:
        - success
